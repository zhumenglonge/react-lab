/* =========================================================
 * ⚠️ 避雷 & 分角色：别问什么、面对不同人问什么
 * ========================================================= */

const AVOID = [
  { t: '什么都不问', d: '等于当面说"我没兴趣 / 没准备"，是最亏的一种。实在紧张，至少备一条兜底（如团队技术栈）。' },
  { t: '问官网 / JD 上查得到的信息', d: '暴露没做功课。提前查过公司产品和业务，问点查不到的。' },
  { t: '一面就主动谈薪资、年假、加班费', d: '技术面问这些时机不对，容易显得只关心钱。薪酬福利留给 HR 面。' },
  { t: '太空泛的大词', d: '如"公司三年战略是什么"，面试官也不好答。换成具体、你能听进去的。' },
  { t: '一上来就问"我过了吗 / 我表现咋样"', d: '把尴尬甩给面试官。想知道结果，问"后续流程和时间"更得体。' },
]

// 面对不同身份，重心不同
const ROLES = [
  ['一面 / 技术面（未来同事）', '技术栈、架构、工程流程、协作节奏、这个岗位要解决的问题', '薪资、晋升、假期'],
  ['主管 / 负责人面', '团队方向、对这个角色的期待、业务定位、成长路径', '过于细枝末节的技术实现'],
  ['HR / 人力面', '公司文化、培养机制、晋升与绩效、流程与时间安排、（这时可）薪酬结构', '深度技术挑战细节'],
]

const TIPS = [
  '准备 3~5 条，当场挑 2~3 条问，其余留作"还有什么想了解的"时的备用。',
  '把"个人体验型"问题（"你在这最有成就感的是什么"）抛给面试官本人，常能得到真诚回答，也拉近距离。',
  '边听边记，追问要接得住对方上一个回答——反问是双向对话，不是走流程。',
]

export default function AvoidRole() {
  return (
    <div className="demo-wrap rvq-root rvq-avoid">
      <section className="rvq-block">
        <h3>🚫 一面尽量别问这些</h3>
        <ul className="rvq-list">
          {AVOID.map((a) => (
            <li key={a.t}>
              <b>{a.t}</b>
              {a.d}
            </li>
          ))}
        </ul>
      </section>

      <section className="rvq-block">
        <h3>👥 面对不同面试官，问不同的</h3>
        <table className="rvq-table">
          <thead>
            <tr>
              <th>对象</th>
              <th>✅ 适合问</th>
              <th>⛔ 先别问</th>
            </tr>
          </thead>
          <tbody>
            {ROLES.map((r) => (
              <tr key={r[0]}>
                <td>{r[0]}</td>
                <td>{r[1]}</td>
                <td className="rvq-td-avoid">{r[2]}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </section>

      <section className="rvq-block">
        <h3>🧷 实操建议</h3>
        <ul className="rvq-list rvq-tips">
          {TIPS.map((t) => (
            <li key={t}>{t}</li>
          ))}
        </ul>
      </section>
    </div>
  )
}
